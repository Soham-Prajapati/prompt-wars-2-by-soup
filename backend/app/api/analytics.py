"""
Analytics API — placeholder module.

Analytics endpoints are served from ``app.api.core`` alongside other
platform-level routes.  This module exists to preserve the package
structure and allow future expansion of analytics-specific routes
(e.g. dashboards, export, etc.).
"""
